# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-preview
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Env
.env
.env.local
.env.production
.env.*.local

# Cloudflare mTLS / 비밀 자산 — 절대 커밋 금지
.secrets/

# Cloudflare Wrangler 로컬 상태
.wrangler/
cloudflare/workers/*/.wrangler/
cloudflare/workers/*/node_modules

# QA screenshots (raw — 루트에 되는 것만)
/qa_*.png
# submission 아래의 PNG 는 제출용이니 tracked

# Phase 7 제출용 아티팩트 (zip/build-report 는 매번 재생성; lighthouse-summary 는 tracked)
dist-submit/
build-report.json


# Phase 8.0-d — local dev QA artifacts (Playwright screenshots etc.)
qa-phase8/

# Phase 8.1 — downloaded raw sound source previews (regenerated by scripts/process-sounds.mjs)
tmp/

# Apps in Toss .ait artifact (regenerated by `npm run build:ait`)
*.ait
.granite/
